overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  src/generated/index.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
