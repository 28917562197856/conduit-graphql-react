mutation addArticle(
  $title: String!
  $description: String!
  $body: String!
  $tagList: [String]
  $userId: Int!
) {
  addArticle(
    title: $title
    description: $description
    body: $body
    tagList: $tagList
    userId: $userId
  ) {
    title
  }
}
